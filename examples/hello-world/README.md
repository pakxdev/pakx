# hello-world

A minimal pakx skill template. See [`SKILL.md`](./SKILL.md) for the
publishing walkthrough.

The CLI reads the YAML frontmatter at the top of `SKILL.md`; the body
is the skill's actual prose, surfaced to agents at install time.
